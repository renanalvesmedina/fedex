package com.mercurio.lms.gm.action;

import java.util.Map;

import com.mercurio.adsm.framework.model.CrudAction;
import com.mercurio.lms.gm.model.service.RotaEmbarqueService;

/**
 * Generated by: ADSM ActionGenerator
 *  
 * Não inserir documentação após ou remover a tag do XDoclet a seguir.
 * O valor do <code>id</code> informado abaixo deve ser utilizado para referenciar este serviço.
 * @spring.bean id="lms.gm.rotaEmbarqueAction"
 */
public class RotaEmbarqueAction extends CrudAction {
	private RotaEmbarqueService rotaEmbarqueService;
	
	public RotaEmbarqueService getRotaEmbarqueService() {
		return rotaEmbarqueService;
	}

	public void setRotaEmbarqueService(RotaEmbarqueService rotaEmbarqueService) {
		this.rotaEmbarqueService = rotaEmbarqueService;
	}



	//Valida RotaMaster para a pesquisa de rota
	public Map<String, Object> findRotaMasterBySiglaDescricao(Map map){	
		String siglaRotaMaster = (String)map.get("siglaRota");
		String nomeRotaMaster = (String)map.get("nomeRota");
		
		if(siglaRotaMaster.equals("")){
			siglaRotaMaster = null;
		}
		if(nomeRotaMaster.equals("")){
			nomeRotaMaster = null;
		}
	
		
		return rotaEmbarqueService.findRotaMasterBySiglaDescricao(siglaRotaMaster, nomeRotaMaster);    	
    }
}
