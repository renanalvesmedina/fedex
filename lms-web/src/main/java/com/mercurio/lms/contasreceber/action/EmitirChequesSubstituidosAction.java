package com.mercurio.lms.contasreceber.action;

import java.util.List;
import java.util.Map;

import com.mercurio.adsm.framework.report.ReportActionSupport;
import com.mercurio.adsm.framework.session.SessionContext;
import com.mercurio.adsm.framework.util.TypedFlatMap;
import com.mercurio.lms.configuracoes.model.Moeda;
import com.mercurio.lms.contasreceber.report.EmitirChequesSubstituidosService;
import com.mercurio.lms.municipios.model.Filial;
import com.mercurio.lms.municipios.model.service.FilialService;
import com.mercurio.lms.util.session.SessionKey;
import com.mercurio.lms.util.session.SessionUtils;

/**
 * Generated by: ADSM ActionGenerator
 *  
 * Não inserir documentação após ou remover a tag do XDoclet a seguir.
 * O valor do <code>id</code> informado abaixo deve ser utilizado para referenciar este serviço.
 * @spring.bean id="lms.contasreceber.emitirChequesSubstituidosAction"
 */

public class EmitirChequesSubstituidosAction extends ReportActionSupport {
	
	private FilialService filialService;

	public void setEmitirChequesSubstituidosService(EmitirChequesSubstituidosService emitirChequesSubstituidosService) {
		this.reportServiceSupport = emitirChequesSubstituidosService;
	}
	
	/**
	 * Busca as filiais para a lookup de filial
	 * @param map Critérios de pesquisa
	 * @return Lista de Filiais
	 */
	public List findLookupFilial(Map map){		
		return filialService.findLookupBySgFilial(map);
	}

	public void setFilialService(FilialService filialService) {
		this.filialService = filialService;
	}
	
    /**
     * Busca a moeda do usuario logado
     * @return Moeda
     */
    public Moeda findMoedaUsuario(){
    	return SessionUtils.getMoedaSessao();
    } 

	public boolean validateFilialUsuarioMatriz(TypedFlatMap tfm){    	
    	return SessionUtils.isFilialSessaoMatriz();
    }
    
    public Filial findFilialUsuarioLogado() {
    	Filial filial = (Filial)SessionContext.get(SessionKey.FILIAL_KEY);
    	filial.setFilialByIdFilialResponsavalAwb(null);
    	filial.setEmpresa(null);
    	return filial;
    }
}