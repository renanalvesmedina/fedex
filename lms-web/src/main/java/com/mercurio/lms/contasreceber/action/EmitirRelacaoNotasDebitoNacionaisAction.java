package com.mercurio.lms.contasreceber.action;

import java.util.List;

import com.mercurio.adsm.framework.report.ReportActionSupport;
import com.mercurio.adsm.framework.util.TypedFlatMap;
import com.mercurio.lms.contasreceber.report.EmitirRelacaoNotasDebitoNacionaisService;
import com.mercurio.lms.municipios.model.Filial;
import com.mercurio.lms.municipios.model.service.FilialService;
import com.mercurio.lms.util.session.SessionUtils;

/**
 * Generated by: ADSM ActionGenerator
 *  
 * Não inserir documentação após ou remover a tag do XDoclet a seguir.
 * O valor do <code>id</code> informado abaixo deve ser utilizado para referenciar este serviço.
 * @spring.bean id="lms.contasreceber.emitirRelacaoNotasDebitoNacionaisAction"
 */

public class EmitirRelacaoNotasDebitoNacionaisAction extends ReportActionSupport {

	FilialService filialService;
	
	
	
	public void setFilialService(FilialService filialService) {
		this.filialService = filialService;
	}



	public void setEmitirRelacaoNotasDebitoNacionaisService(EmitirRelacaoNotasDebitoNacionaisService emitirRelacaoNotasDebitoNacionaisService) {
		this.reportServiceSupport = emitirRelacaoNotasDebitoNacionaisService;
	}
	
	
    public List findLookupFilial(TypedFlatMap map){
		return filialService.findLookup(map);
	}

	public TypedFlatMap findFilialUsuarioLogado(){
		TypedFlatMap tfm = new TypedFlatMap();
		Filial filial = SessionUtils.getFilialSessao();
		tfm.put("filialCobranca.idFilial",filial.getIdFilial());
		tfm.put("filialCobranca.sgFilial",filial.getSgFilial());
		tfm.put("filialCobranca.pessoa.nmPessoa",filial.getPessoa().getNmFantasia());
		return tfm;
	}
	
}
