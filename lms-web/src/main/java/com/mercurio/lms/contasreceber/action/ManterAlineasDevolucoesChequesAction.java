package com.mercurio.lms.contasreceber.action;

import java.io.Serializable;
import java.util.List;

import com.mercurio.adsm.framework.model.CrudAction;
import com.mercurio.lms.contasreceber.model.Alinea;
import com.mercurio.lms.contasreceber.model.service.AlineaService;

import com.mercurio.adsm.framework.annotations.ParametrizedAttribute;


/**
 * Generated by: ADSM ActionGenerator
 *  
 * Não inserir documentação após ou remover a tag do XDoclet a seguir.
 * O valor do <code>id</code> informado abaixo deve ser utilizado para referenciar este serviço.
 * @spring.bean id="lms.contasreceber.manterAlineasDevolucoesChequesAction"
 */

public class ManterAlineasDevolucoesChequesAction extends CrudAction {

    public void setAlineaService(AlineaService alineaService){
		this.defaultService = alineaService;
	}

    /**
	 * Insere, caso o id seja <code>null</code> ou atualiza uma entidade, caso contrário.
	 * @param bean entidade a ser armazenada.
	 * @return entidade que foi armazenada.
	 */
    public Serializable store(Alinea bean) {
        return ((AlineaService)this.defaultService).store(bean);
    }
    
    /**
     * Remove uma Alinea pelo id passado mpor parametro
     * @param id
     */
    public void removeById(Long id) {
		((AlineaService)this.defaultService).removeById(id);
	}
	
	/**
	 *
	 */
	@ParametrizedAttribute(type = java.lang.Long.class)
	public void removeByIds(List ids) {
		((AlineaService)this.defaultService).removeByIds(ids);
	}
    
	/**
	 * Busca uma Alinea de acordo com o id passado por parametro
	 * @param id
	 * @return
	 */
	public Serializable findById(Long id) {
		return ((AlineaService)this.defaultService).findById(id);
	}
    
	
}
