package com.mercurio.lms.tabelaprecos.action;

import java.io.Serializable;
import java.util.List;

import com.mercurio.adsm.framework.annotations.ParametrizedAttribute;
import com.mercurio.adsm.framework.model.CrudAction;
import com.mercurio.adsm.framework.util.TypedFlatMap;
import com.mercurio.lms.tabelaprecos.model.PrecoFrete;
import com.mercurio.lms.tabelaprecos.model.service.PrecoFreteService;
import com.mercurio.lms.tabelaprecos.model.service.TabelaPrecoService;

/**
 * Generated by: ADSM ActionGenerator
 *  
 * Não inserir documentação após ou remover a tag do XDoclet a seguir.
 * O valor do <code>id</code> informado abaixo deve ser utilizado para referenciar este serviço.
 * @spring.bean id="lms.tabelaprecos.manterPrecosFretesAction"
 */

public class ManterPrecosFretesAction extends CrudAction {
	
	private TabelaPrecoService tabelaPrecoService;
	
	public void setPrecoFrete(PrecoFreteService precoFreteService) {
		this.defaultService = precoFreteService;
	}
    public void removeById(java.lang.Long id) {
        ((PrecoFreteService)defaultService).removeById(id);
    }

	/**
	 *
	 */
	@ParametrizedAttribute(type = java.lang.Long.class)
    public void removeByIds(List ids) {
    	((PrecoFreteService)defaultService).removeByIds(ids);
    }

    public PrecoFrete findById(java.lang.Long id) {
    	return ((PrecoFreteService)defaultService).findById(id);
    }
    
	public void setTabelaPrecoService(TabelaPrecoService tabelaPrecoService) {
		this.tabelaPrecoService = tabelaPrecoService;
	}
	
	public Serializable storeAtualizaTabela(PrecoFrete bean) {
		TypedFlatMap toReturn = new TypedFlatMap();
		
		toReturn.put("idPrecoFrete", this.store(bean));
		PrecoFrete beanRetorno = this.findById(bean.getIdPrecoFrete());
		
		Long idTabelaPrecoParcela = (beanRetorno.getTabelaPrecoParcela() != null && beanRetorno.getTabelaPrecoParcela().getIdTabelaPrecoParcela() != null) ? beanRetorno.getTabelaPrecoParcela().getIdTabelaPrecoParcela() : null;
		String msgAtualizacaoAutomatica = tabelaPrecoService.findAtualizaTabelaCustos(idTabelaPrecoParcela);

		toReturn.put("msgAtualizacaoAutomatica", msgAtualizacaoAutomatica);
		return toReturn;
	}

}
