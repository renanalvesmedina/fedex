package com.mercurio.lms.entrega.action;

import java.util.List;
import java.util.Map;

import com.mercurio.adsm.framework.report.ReportActionSupport;
import com.mercurio.adsm.framework.util.TypedFlatMap;
import com.mercurio.lms.entrega.report.EmitirContabilizacaoEntregasService;
import com.mercurio.lms.municipios.model.Filial;
import com.mercurio.lms.municipios.model.service.FilialService;
import com.mercurio.lms.util.JTDateTimeUtils;
import com.mercurio.lms.util.session.SessionUtils;

/**
 * Generated by: ADSM ActionGenerator
 *  
 * Não inserir documentação após ou remover a tag do XDoclet a seguir.
 * O valor do <code>id</code> informado abaixo deve ser utilizado para referenciar este serviço.
 * @spring.bean id="lms.entrega.emitirContabilizacaoEntregasAction"
 */

public class EmitirContabilizacaoEntregasAction extends ReportActionSupport {
	
	private FilialService filialService;
	
	
	public void setEmitirContabilizacaoEntregasService(EmitirContabilizacaoEntregasService emitirContabilizacaoEntregasService) {
		this.reportServiceSupport = emitirContabilizacaoEntregasService;
	}

	public List findLookupFilial(Map criteria) {
		return filialService.findLookupFilial(criteria);
	}
	
	public void setFilialService(FilialService filialService) {
		this.filialService = filialService;
	}

	
	public TypedFlatMap findDataSession() {
    	Filial  filial  = SessionUtils.getFilialSessao();
    	TypedFlatMap result = new TypedFlatMap();
		result.put("filial.idFilial",filial.getIdFilial());
		result.put("filial.sgFilial",filial.getSgFilial());
		result.put("filial.pessoa.nmFantasia",filial.getPessoa().getNmFantasia());
		result.put("dhIni",JTDateTimeUtils.getDataAtual().minusDays(30));
		result.put("dhFim",JTDateTimeUtils.getDataAtual());
    	return result;
    }
}
